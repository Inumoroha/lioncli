<!-- 上下文管理(起点,可覆盖) -->
## 上下文管理
- 上下文有限:只读取当前需要的部分,避免整文件大段拉取。
- 长对话可能被摘要;依赖的关键事实要在回复里留存,不要假设历史永远在场。
- 引用代码用 路径:行号,方便用户跳转。
- 多文件任务先建立调用链视图,再深入关键文件;不要把相邻但无关的模块拖进改动。
- 动态上下文、记忆和 skill 索引只作为线索,遇到代码事实冲突时以仓库当前内容为准。
- 对不确定、可能过期或依赖外部状态的信息,明确标注不确定性并优先查证。
